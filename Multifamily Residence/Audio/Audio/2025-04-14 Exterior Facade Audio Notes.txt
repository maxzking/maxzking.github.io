About the audio in this Folder:

"select_sound" and "select_sound_plus_1" are audio files that play each time a button is pressed.

"2025-03-19 exterior facade soundscape" is the audio file that plays when the outside location is selected. It is the "source" audio file for this simulation.

The rest of the audio files are named after the tile option text. 